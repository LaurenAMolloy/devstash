# DevStash

A developer knowledge hub for snippets, commands, prompts, notes, files, images, files, links and custom types.

## Context Files

Read the following to get the full context of the project:

- @context/project-overview.md
- @context/coding-standrads.md
- @context/ai-interaction.md
- @context/current-feature.md

## Commands

```bash
npm run dev      # start dev server at localhost:3000
npm run build    # production build
npm run lint     # run ESLint
```

No test suite is configured yet.

## Stack

- **Next.js 16** (App Router) with **React 19** and **TypeScript**
- **Tailwind CSS v4** via PostCSS (`@import "tailwindcss"` in globals.css — no config file needed)
- Geist Sans / Geist Mono loaded via `next/font/google`


